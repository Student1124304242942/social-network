specVersion: "v1beta"
serviceId: "react-social-network"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-social-network-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
